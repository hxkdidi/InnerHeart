package daixin.me.bugua.view;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

/**
 * Created by shidai on 2016/5/8.
 */
public class CusPlayImage extends View{

    public CusPlayImage(Context context) {
        this(context,null);
    }

    public CusPlayImage(Context context, AttributeSet attrs) {
        this(context, attrs,0);
    }

    public CusPlayImage(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
        init(context);
    }

    private void init(Context context) {

    }
}
